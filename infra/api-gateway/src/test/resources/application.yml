server:
  port: 8080

spring:
  cloud:
    config:
      enabled: false

app:
  security:
    users:
      - login: ivan
        password: ivan
        roles:
          - USER
      - login: anna
        password: anna
        roles:
          - USER
          - ADMIN
